def generate_random_code():
    # Example code to simulate generating a random integer within a given range
    import random
    start = 10
    end = 50
    code = f"start: {start}, end: {end}"
    return code
